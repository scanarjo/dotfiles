- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.gitconfig:
    ~/.zshrc:
#     ~/.vim: vim

- create:
    - ~/Repos
    - ~/Pictures/Screenshots

- shell:
  - 
    command: ./setup_homebrew.zsh
    stderr: true
    stdout: true
