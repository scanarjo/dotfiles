- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc: zshrc
#     ~/.vim: vim

- create:
    - ~/Repos
    - ~/Pictures/Screenshots

# - shell:
#   - [git submodule update --init --recursive, Installing submodules]
